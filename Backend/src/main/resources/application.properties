spring.application.name=apoflow-backend
server.port=8080

# MongoDB Configuration
spring.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/apoflow}

# OAuth2 client auto-config is disabled until real client credentials are provided.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration

# JWT Configuration
jwt.secret=${JWT_SECRET:kP9#mX2!vL7$qZ4*bN1&fJ8@wR3%tY6_xC5}
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:80
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=3600

# Application base URL (used in password-reset links)
app.base-url=${APP_BASE_URL:http://localhost:8080}

# MailerSend configuration
mailersend.token=${MAILERSEND_TOKEN:mlsn.2027e4c2eb2e848b3e5905ce5324056980660cbfd8497698fc7bb482cc8a6edd}
mailersend.from=${MAILERSEND_FROM:MS_apoflow@test-3m5jgro50ymgdpyo.mlsender.net}
app.email.enabled=${EMAIL_ENABLED:true}

